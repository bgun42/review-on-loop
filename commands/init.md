---
description: Initialize Veriloop role models, strict blind mode, and findings state
argument-hint: [optional role=model pairs]
---

# Init

Invoke the bundled `initialize-review-loop` skill with `$ARGUMENTS`.

Treat `skills/initialize-review-loop/SKILL.md` as the single source of truth. Preserve
strict blind mode as the only persistent mode and do not overwrite an existing ledger.
