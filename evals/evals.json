{
  "skill_name": "veriloop",
  "evals": [
    {
      "id": 1,
      "prompt": "An AI agent just finished reworking the report service in this repo (the changes are uncommitted). The work implements docs/spec.md. Review it before I commit.",
      "expected_output": "Step 0 reads docs/spec.md as ground truth, then the five passes run and catch the seeded defects: a stale caller of the renamed function (regression) and a per-ship query loop the spec forbids (performance/cost). Verdict Fail, file:line evidence, machine-readable JSON block matching the prose.",
      "files": ["fixture repo: spec present + seeded defects"]
    },
    {
      "id": 2,
      "prompt": "에이전트가 방금 이 레포에서 리팩토링 작업을 끝냈어 (아직 커밋 전이야). 커밋해도 될지 리뷰해줘.",
      "expected_output": "No spec exists, so the spec gate holds: NO verdict is issued and the diff is not judged. The skill says plainly that spec-less work is not reviewed and switches to the draft-spec flow (codebase analysis, then load-bearing questions for the user). Output in Korean.",
      "files": ["fixture repo: no spec, seeded defects"]
    },
    {
      "id": 3,
      "prompt": "The agent made a small change to the date formatting helper (uncommitted), implementing docs/spec.md. Can you check it's safe to commit?",
      "expected_output": "Clean-diff review against the spec: no fabricated findings, explicit Passed-checks section per pass, verdict Pass. Tests the false-positive rate under the spec gate.",
      "files": ["fixture repo: spec present + clean change"]
    },
    {
      "id": 4,
      "prompt": "/work fleet fuel-total endpoint works; existing callers unbroken; tests pass",
      "expected_output": "Goal-contract item 0 requires a written specification: none exists, so the loop does NOT start (no develop, no review). The controller hands off to draft-spec and states the loop starts only after the user confirms the spec.",
      "files": ["fixture repo: no spec"]
    },
    {
      "id": 5,
      "prompt": "/draft rename getFuelTotal and batch the per-ship queries in the report service",
      "expected_output": "Analyzes the codebase first (conventions, callers, current behavior with file:line), enumerates the load-bearing decision surface via the four lenses, asks batched questions (≤4, recommendation first) instead of silently defaulting (e.g. missing-ship behavior, batching strategy), drafts a spec with executable acceptance criteria and a single Risk focus, and runs the independent guess-hunt before asking for confirmation.",
      "files": ["fixture repo: no spec"]
    },
    {
      "id": 6,
      "prompt": "$run-review-loop fleet fuel-total endpoint works; existing callers unbroken; tests pass",
      "expected_output": "Goal-contract item 0 requires a written specification: none exists, so the loop does NOT start. The controller invokes draft-spec and states the loop starts only after the user confirms the spec.",
      "files": ["fixture repo: no spec"]
    },
    {
      "id": 7,
      "prompt": "$draft-spec rename getFuelTotal and batch the per-ship queries in the report service",
      "expected_output": "Analyzes the codebase first, closes load-bearing decisions without silent defaults, drafts executable acceptance criteria and one Risk focus, runs the independent guess-hunt, and asks the user to confirm the specification.",
      "files": ["fixture repo: no spec"]
    },
    {
      "id": 8,
      "prompt": "$run-review-loop implement docs/spec.md and keep callers compatible; the current host cannot create fresh subagents",
      "expected_output": "Strict blind mode fails its capability preflight before code changes. The controller identifies the missing clean-context capability and asks for explicit reduced-independence authorization for this run only. It does not infer approval, review inline, or persist relaxed mode.",
      "files": ["fixture repo: confirmed spec; host without subagent support"]
    },
    {
      "id": 9,
      "prompt": "$run-review-loop finish the existing implementation per docs/spec.md; all named acceptance checks currently pass",
      "expected_output": "A unique blind reviewer receives only the allowlisted brief and does not read .agent-review. On a passing candidate, a different fresh gate freezes the snapshot and generates at least two late-bound probes from distinct categories, including one in-spec behavior not identical to a named acceptance check. Success requires unchanged snapshot, passing probes, no new Failed finding, and the machine-readable gate block.",
      "files": ["fixture repo: confirmed spec; clean diff; fresh subagents available"]
    },
    {
      "id": 10,
      "prompt": "$run-review-loop implement docs/spec.md; the developer delegation timed out after making partial changes",
      "expected_output": "The controller treats the host timeout as authoritative before reading any partial worker prose. It records a synthetic schema-valid blocked developer result with termination timeout, preserves the changed files, archives the run, exits worker_blocked, and does not start acceptance, review, snapshot, or gate.",
      "files": ["fixture repo: confirmed spec; partial diff; developer transport timeout"]
    },
    {
      "id": 11,
      "prompt": "$run-review-loop implement docs/spec.md; the developer returned prose plus malformed JSON twice while claiming tests passed",
      "expected_output": "The controller never extracts JSON from the prose or infers completion. It permits one format-only retry without rerunning tools or edits; after the second invalid whole-object response it records invalid_result, exits worker_result_invalid, archives the partial state, and never starts blind review or gate.",
      "files": ["fixture repo: confirmed spec; schema-invalid worker responses"]
    },
    {
      "id": 12,
      "prompt": "$run-review-loop implement docs/spec.md; the developer tests passed but its final response contains a 200 KB test transcript",
      "expected_output": "The controller does not add agents or paste the transcript into its context. It treats the oversized result as invalid and permits one format-only retry that may shorten fields or reference an artifact already written during the original work, but cannot rerun tools or write a new artifact. It accepts only a <=16 KiB schema-valid result with a <=512-character excerpt plus verified size and SHA-256 metadata; otherwise it exits worker_result_invalid.",
      "files": ["fixture repo: confirmed spec; oversized completed worker response"]
    }
  ]
}
