interface:
  display_name: "Initialize Review Loop"
  short_description: "Configure strict blind review-loop roles"
  default_prompt: "Use $initialize-review-loop to configure strict blind review and role models for this repository."
